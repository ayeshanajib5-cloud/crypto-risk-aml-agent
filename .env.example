# Copy this to .env and fill in real values
# Never commit .env to git

# Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
RAW_TRADES_TOPIC=raw-trades
RISK_SIGNALS_TOPIC=market-risk-signals
AML_ALERTS_TOPIC=aml-alerts
COMPLIANCE_EVENTS_TOPIC=compliance-events

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=crypto_risk
POSTGRES_USER=riskadmin
POSTGRES_PASSWORD=changeme_in_prod

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Claude API (added in a later step)
ANTHROPIC_API_KEY=sk-ant-...

# MLflow
MLFLOW_TRACKING_URI=http://localhost:5001
